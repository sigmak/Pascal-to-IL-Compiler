// ============================================================
// Stage 35 진단 테스트 (2): 구문 분석 단계 오류가 정확한 줄/열(캐럿 ^)로 표시되는지 확인.
// 이 파일은 일부러 깨져 있습니다 — 컴파일하면 실패해야 정상입니다.
// (참고: 이 파서는 문장 사이 세미콜론을 생략해도 에러로 보지 않습니다 — 있으면 소비하고
//  없으면 그냥 다음 문장으로 넘어갑니다. 그래서 세미콜론 누락으로는 에러를 못 냅니다.
//  대신 '+' 뒤에 식이 와야 하는데 안 온 경우로 진짜 구문 오류를 만듭니다.)
// 기대 동작: 5번째 문장의 ';' 위치(= '+' 다음에 식이 와야 하는데 세미콜론이 온 지점)를
//           가리키며 정확한 줄/열에 캐럿(^)이 표시되어야 합니다.
// ============================================================
program Stage35ParseErrorTest;

var
  x: integer;

begin
  x := 1 + ;
  writeln(x);
end.