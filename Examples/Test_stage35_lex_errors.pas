// ============================================================
// Stage 35 진단 테스트 (1): 어휘 분석 단계에서 여러 오류를 한 번에 모아 보고하는지 확인.
// 이 파일은 일부러 깨져 있습니다 — 컴파일하면 실패해야 정상입니다.
// 기대 동작: '@'와 '$' 두 개의 알 수 없는 문자 오류가 각각의 줄/열 정보와 함께
//           한 번의 컴파일 시도에서 모두 보고되어야 합니다 (첫 번째에서 멈추면 안 됨).
// ============================================================
program Stage35LexErrorTest;

var
  x: integer;

begin
  x := 1 @ 2;
  x := x $ 3;
  writeln(x);
end.